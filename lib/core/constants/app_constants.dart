/// Shared application constants and default values.
class AppConstants {
  const AppConstants._();

  static const String appName = 'Norivo';
  static const double defaultPadding = 16.0;
  static const double defaultRadius = 12.0;
}
